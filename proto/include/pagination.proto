syntax = "proto3";

package pagination;

option go_package = "github.com/iotexproject/iotex-analyser-api/api/pagination";

message Pagination {
    uint64 skip = 1;// starting index of results
    uint64 first = 2; // number of records per page
    string order = 3; 
  }